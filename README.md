## This repo contains following

- aks cluster with KEDA
- helmfile to install argocd, clusterautoscaler, GHA runner, AZ devops runner, wireguard VPN with bastion,
- build a java package [Quartz MongoDB](https://github.com/michaelklishin/quartz-mongodb/blob/v2.2.0-rc2/build.gradle) and push to azure artifacts,
- build docker image, push to azure repos, after scanning with trivy
- 
